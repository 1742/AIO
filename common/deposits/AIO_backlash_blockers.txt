# Blocker left behind by a reality interference backlash. Only events place it,
# so it never spawns on its own. Icon is borrowed from a vanilla blocker until a
# dedicated texture exists.

d_AIO_energy_cascade_residue = {
	time = 360
	icon = d_rotten_soil_blocker

	category = deposit_cat_blockers

	potential = { always = no }

	planet_modifier = {
		planet_jobs_energy_produces_mult = -0.05
	}

	resources = {
		category = deposit_blockers
		cost = {
			energy = 1000
		}
	}

	ai_weight = {
		weight = 2
	}
}

# The balanced accident leaves this instead. Same shape as the energy residue,
# but the ground is flattened rather than charged: nothing can be built to stand
# out on it.
d_AIO_balanced_uniform_residue = {
	time = 360
	icon = d_crater

	category = deposit_cat_blockers

	potential = { always = no }

	planet_modifier = {
		planet_max_districts_add = -1
	}

	resources = {
		category = deposit_blockers
		cost = {
			energy = 1000
		}
	}

	ai_weight = {
		weight = 2
	}
}

# The ecology accident leaves ground that the runaway biosphere has taken over
# and made useless for anything else.
d_AIO_ecology_overgrowth = {
	time = 360
	icon = d_dense_jungle

	category = deposit_cat_blockers

	potential = { always = no }

	planet_modifier = {
		planet_jobs_food_produces_mult = -0.05
	}

	resources = {
		category = deposit_blockers
		cost = {
			energy = 1000
		}
	}

	ai_weight = {
		weight = 2
	}
}
