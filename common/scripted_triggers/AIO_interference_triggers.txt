# Search system within owner's range, excluding self.
# Scope: system.
AIO_is_nearby_interference_source = {
	closest_system = {
		use_bypasses = no
		min_steps = 1
		max_steps = $OWNER$.AIO_interference_range
		valid = {
			has_psionic_aura = AIO_reality_interference_aura
			aura_owner = { is_same_value = $OWNER$ }
		}
		limit = { has_star_flag = AIO_interference_source }
	}
}

# Scope: system. Includes both planetary and ship colonies.
AIO_interference_has_colony = {
	OR = {
		any_system_planet_colony = { always = yes }
		any_system_ship_colony = { always = yes }
	}
}
